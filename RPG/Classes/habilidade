class Habilidade:
    def __init__(self, id_habilidade, nome, descricao, tipo_habilidade, custo, alcance, dano, tempo_recarga, alvo):
        self.id_habilidade = id_habilidade
        self.nome = nome
        self.descricao = descricao
        self.tipo_habilidade = tipo_habilidade
        self.custo = custo
        self.alcance = alcance
        self.dano = dano
        self.tempo_recarga = tempo_recarga
        self.alvo = alvo

    def executar(self, alvo):
        """Executa a habilidade em um alvo."""
        pass

    def aplicar_efeito(self, alvo):
        """Aplica o efeito da habilidade em um alvo."""
        pass

    def calcular_dano(self):
        """Calcula o dano da habilidade."""
        pass